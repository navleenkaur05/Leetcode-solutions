class Solution {
    public int countCommas(int n) {
        return Math.max(0,n-999);
        // int count=0;
        // for(int i=0;i<=n;i++){
        //     if(i>=1000) count++;
        // }
        // return count;
    }
}